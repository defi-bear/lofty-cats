const baseUrl = 'http://catstagram.lofty.codes/api/';
const endpoints = {
  // API,
  users: `${baseUrl}users/`,
  posts: `${baseUrl}posts`,
  comments: `${baseUrl}comments`,
};

export default endpoints;
